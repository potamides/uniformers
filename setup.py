from setuptools import setup

# For the time being, pip still might require a setup.py file to support
# editable installs (see
# https://setuptools.pypa.io/en/latest/userguide/pyproject_config.html).
setup()
